import React from 'react';

const Settings = () => {
    return (
        <div className="container mt-5">
            <h1>Settings</h1>
            <p>Configure application settings and manage admin accounts here.</p>
        </div>
    );
};

export default Settings;